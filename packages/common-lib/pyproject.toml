[project]
name = "math-practice"
version = "0.1.0"
description = "Shared library for adaptive math practice (engine, models, config)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/math_practice"]
