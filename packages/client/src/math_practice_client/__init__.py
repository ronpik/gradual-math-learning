"""math_practice_client: HTTP client / CLI for the math-practice backend.

Talks to the backend over HTTP only (no dependency on the engine library). The
interactive practice loop and synthetic ``--auto`` student are built in a later
phase; this package currently exposes a placeholder entry point.
"""

__version__ = "0.1.0"
