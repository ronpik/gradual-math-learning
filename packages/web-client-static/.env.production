# Production build config for the static web client.
# Vite auto-loads this file for `npm run build` (mode=production) and inlines
# VITE_* vars into the bundle. Set this to the backend's PUBLIC origin, then
# rebuild — the value is baked in at build time, not read at runtime.
VITE_API_BASE=https://api.your-domain.com
